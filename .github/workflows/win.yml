name: test on windows

on:
  repository_dispatch:
  push:
    branches:
      - master
  schedule:
    - cron:  '0 4 * * *'

jobs:
  build:

    runs-on: windows-latest

    steps:
    - name: Run vue installer
      timeout-minutes: 20
      run: npx isomor-vue-app my-app
      env:
        COLUMNS: 50
        LINES: 20
