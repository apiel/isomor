
export default function({ types: t }) {
    console.log('t', t);
    return {
        visitor: {
            // Identifier(path, state) { },
            // ASTNodeTypeHere(path, state) { }
        },
    };
}
